# Deferred Items

Items intentionally held for later — not forgotten.

## Formspree Form ID
Contact form is wired to Formspree via `partials/contact-form.js`. Replace `YOUR_FORMSPREE_ID` in `partials/contact-config.js` with your Formspree form ID. In Formspree dashboard, set the notification email to `ruby@spokendevotion.com`.

## Social Media Links (item 8)
Homepage social icons still point to `#`. Update with real profile URLs when ready.

## Instrumental Tracks (item 13)
Instrumental page is live in navigation with a "coming soon" message. Add real tracks when audio files are ready.

## YouTube Migration for Audio (item 16)
Plan to move hosted MP3s to YouTube and embed players instead of serving audio files directly from the repo.

## www DNS
Add CNAME record at domain registrar: `www` → `awsimkins.github.io`.

## Page Banner Images
Add banner PNGs under `/images/` for worship, prayer, confessions, bible, and instrumental pages (homepage uses `main.png`).